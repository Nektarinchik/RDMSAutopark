@model IEnumerable<Autopark.WEB.Entities.Generation>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-area="Administration" asp-controller="Generations" asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Model.Manufacturer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Model)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>


            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Model.Manufacturer.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Model.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                <a asp-area="Administration" asp-controller="Generations" asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-area="Administration" asp-controller="Generations" asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-area="Administration" asp-controller="Generations" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
